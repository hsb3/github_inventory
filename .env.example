# GitHub Inventory Configuration
# Copy this file to .env and update with your values

# Default GitHub username
GITHUB_USERNAME=your-github-username

# Default output file paths (will be dynamically updated based on username)
OWNED_REPOS_CSV=docs/{username}/repos.csv
STARRED_REPOS_CSV=docs/{username}/starred_repos.csv
REPORT_OUTPUT_MD=docs/{username}/README.md

# Report display limits (-1 for no limit, default: 30 for owned, 25 for starred)
REPORT_OWNED_LIMIT=-1
REPORT_STARRED_LIMIT=-1

# GitHub CLI settings (optional - uses system gh config by default)
# GITHUB_TOKEN=your_personal_access_token
